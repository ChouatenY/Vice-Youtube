// This file contains constants for the local user
// These are used for the local development environment

// A fixed ID for the local user
export const LOCAL_USER_ID = 'local-user-id';

// A fixed email for the local user
export const LOCAL_USER_EMAIL = 'local-user@example.com';

// Default user object for client-side use
export const DEFAULT_USER = {
  id: LOCAL_USER_ID,
  email: LOCAL_USER_EMAIL,
  name: 'Local User'
};


